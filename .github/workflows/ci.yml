name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

env:
  BETTER_AUTH_SECRET: ${{ secrets.BETTER_AUTH_SECRET }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '26'
          cache: 'npm'

      - run: npm ci

      - run: npm run build

  # e2e and security share the same non-production database (E2E_DATABASE_URL,
  # same as local dev — see security/security.config.js's comment on this).
  # Kept in one sequential job, not parallel jobs, so they never mutate that
  # shared DB (stock, orders) at the same time.
  e2e-and-security:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '26'
          cache: 'npm'

      - run: npm ci

      - run: npx playwright install --with-deps chromium

      - name: Create .env.e2e
        run: echo "E2E_DATABASE_URL=${{ secrets.E2E_DATABASE_URL }}" > .env.e2e

      - run: npm run e2e:db:setup

      - run: npm run test:e2e

      - run: npm run security:test

      - name: Upload e2e/security reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-and-security-reports
          path: |
            playwright-report/
            security/results/
          retention-days: 7

  # Own job (own database, own runner) so a slow/flaky perf run never blocks
  # e2e/security feedback. Runs the fast "smoke" profile, not "load" — the
  # sustained load test is heavier and latency thresholds are more prone to
  # noise on shared GitHub-hosted runners than on a real dev machine.
  perf:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '26'
          cache: 'npm'

      - run: npm ci

      - uses: grafana/setup-k6-action@v1

      - name: Create .env.perf
        run: echo "PERF_DATABASE_URL=${{ secrets.PERF_DATABASE_URL }}" > .env.perf

      - run: npm run perf:db:setup

      - run: npm run perf:smoke

      - name: Upload perf report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: perf-report
          path: perf/results/
          retention-days: 7
